# Protean — Open Issues & Devil's-Advocate Findings

> Surfaced by the `protean-technical-depth` workflow (26 agents). Stub-level ImportError bugs flagged here were FIXED in commit (sampler/manifest/shape_sampler imports). The remaining items are design/implementation risks tracked for build day. Severity 1–5.


## Round 2 — math/verifier attacks

- **[S5] SHAPE_SAMPLER.PY IMPORTS NAMES THAT DO NOT EXIST IN SPLITS.PY OR SAMPLER.PY — GUARANTEED IMPORTERROR AT RUNTIME** — shape_sampler.py line 8 (verified from source): 'from protean.splits import TRAIN_BANDS, HELD_OUT_BANDS, assert_disjoint'. splits.py (verified from source) exports only: TRAIN_M, TEST_M, _assert_split_disjoint (note the leading underscore). TRAIN_BANDS does not exist. HELD_OUT_BANDS does not exist. 
  - **Fix:** Immediate fix required before any import works: in splits.py rename _assert_split_disjoint to assert_disjoint (remove underscore) OR add alias assert_disjoint = _assert_split_disjoint; add TRAIN_BANDS and HELD_OUT_BANDS exports (or change shape_sampler.py to import TRAIN_M/TEST_M
- **[S5] MANIFEST.PY CALLS _assert_split_disjoint(), _atomic_write(), AND sample_task() WITHOUT IMPORTING ANY OF THEM — THREE MISSING IMPORTS GUARANTEE NameError** — manifest.py (verified from source) calls _assert_split_disjoint() at line 5, sample_task() at line 9, and _atomic_write() at line 11. None of these are imported in the manifest.py stub — there are no import statements at all in the file. _assert_split_disjoint is defined in splits.py; sample_task in
  - **Fix:** Add at the top of manifest.py: 'from protean.splits import _assert_split_disjoint' (or the renamed assert_disjoint); 'from protean.sampler import sample_task'; implement _atomic_write(path, rows) explicitly (write to path+'.tmp', call os.replace). Run 'python -c "from protean.man
- **[S5] THE DELEGATES_TO_MATRIX_UNIT CHECK BANS tl.dot FOR MATMUL-CLASS OPS, WHICH WOULD ALSO BAN LEGITIMATE HIGH-PERFORMANCE TRITON MATMUL IMPLEMENTATIONS THAT USE tl.dot AS THEIR PRIMARY COMPUTE PRIMITIVE** — anti_hack.py (verified from source): delegates_to_matrix_unit returns True (and therefore blocks the kernel) if 'tl.dot' appears in the source for matmul-class ops. However, tl.dot is the CANONICAL Triton primitive for matrix multiplication — every high-performance Triton matmul tutorial and KernelB
  - **Fix:** Remove tl.dot from BANNED_CALLS entirely. tl.dot is a Triton language primitive and is the correct way to implement matmul in Triton. If the concern is delegation to non-Triton code, the ban should target 'torch.ops.aten', 'cublas', 'torch.matmul' (already in BANNED_CALLS), not T
- **[S5] FORMULA ERROR — daVinci Eq1 is multiplicative; Protean's additive port breaks the stated derivation** — The spec states 'daVinci PR is an ADDITIVE BONUS (matches Eq1 continuity)' and writes reward = CORRECT_FLOOR + speedup_score + pr_term. This directly contradicts daVinci Eq1, which reads R_{i,t} = C(y_{i,t}) * (1 + speedup_{i,t} + PR_{i,t}). In the paper the entire quantity (1 + speedup + PR) is mul
  - **Fix:** Rename the formula explicitly: R_Protean = CORRECT_FLOOR + clip(speedup, 0, SPEEDUP_CAP)/P_TARGET + PR_BONUS*clip(pr_frac, 0, 1). State in the spec and in the demo slide that this is a HYBRID of Kevin (floor) and daVinci (PR additive bonus), not 'Eq1 verbatim' or 'Eq1 continuity'
- **[S5] IMPORT ERROR — shape_sampler.py raises ImportError at runtime; the moat invariant never fires** — The file /Users/nihalnihalani/Desktop/Github/protean/src/protean/shape_sampler.py line 8 imports: 'from protean.splits import TRAIN_BANDS, HELD_OUT_BANDS, assert_disjoint'. The file /Users/nihalnihalani/Desktop/Github/protean/src/protean/splits.py exports TRAIN_M, TEST_M, and a function named _asser
  - **Fix:** In splits.py: rename _assert_split_disjoint to assert_disjoint (public) and add TRAIN_BANDS, HELD_OUT_BANDS aliases or replace them with the existing TRAIN_M/TEST_M exports — pick one naming convention and apply it everywhere. In sampler.py: add 'from protean.splits import TRAIN_
- **[S5] MANIFEST BUG — manifest.py calls _assert_split_disjoint and sample_task with no imports; the freeze function raises NameError on first call** — manifest.py (confirmed from source at /Users/nihalnihalani/Desktop/Github/protean/src/protean/manifest.py) contains a freeze() function that calls _assert_split_disjoint() and sample_task() on lines 5 and 9 respectively, and _atomic_write() on line 11. None of these three names are imported in the f
  - **Fix:** Add to manifest.py: 'from protean.splits import _assert_split_disjoint; from protean.sampler import sample_task; import json, os'. Implement _atomic_write as: write to path+'.tmp', then os.replace(path+'.tmp', path). Add a test: 'python -m protean.manifest' that calls freeze with
- **[S5] ANTI-HACK LOGIC ERROR — delegates_to_matrix_unit bans tl.dot for matmul-class ops, which also bans legitimate Triton matrix-multiply implementations that require tl.dot** — anti_hack.py lines 27-32: delegates_to_matrix_unit returns True (and therefore blocks the kernel) if 'tl.dot' appears in the source and the op is a matmul-class op. tl.dot is the primary Triton intrinsic for performing a matrix multiply tile — it is the instruction that a correctly-written Triton ma
  - **Fix:** Remove the tl.dot ban entirely. The correct countermeasure for the cuBLAS-delegation exploit on matmul-class ops is: (1) ban torch.matmul, torch.mm, cublas calls (already in BANNED_CALLS); (2) require launches_timed > 0 (already gated); (3) for matmul-class ops, additionally requ
- **[S5] REWARD INCONSISTENCY — three different reward formulas appear across the spec documents with incompatible range bounds and PR integration** — The spec contains three distinct reward formulas that are never reconciled: (1) rewards.py (confirmed source): reward = CORRECT_FLOOR + speedup_score + pr_term, range [0, 2.0]. (2) IMPLEMENTATION_COMPONENTS.md harness pseudocode: 'reward = 1.0 * (1.0 + speedup_norm + pr), in [1.0, 3.0] for correct f
  - **Fix:** Designate rewards.py (formula 1) as the single canonical formula. Strike through or explicitly mark the two alternative formulas in IMPLEMENTATION_COMPONENTS.md as 'SUPERSEDED — see rewards.py'. Add a comment at the top of each superseded pseudocode block: 'IMPLEMENTATION NOTE: t
- **[S4] REWARD FORMULA INTERNAL CONTRADICTION: speedup_score is unbounded above 1.0 when speedup > P_TARGET, but the spec simultaneously claims the reward is bounded [0, 2.0]** — In rewards.py (line 49), speedup_score = min(speedup, SPEEDUP_CAP) / P_TARGET. With SPEEDUP_CAP=20.0 and P_TARGET=1.5, speedup_score = 20.0/1.5 = 13.33. Then reward = CORRECT_FLOOR(0.3) + speedup_score(13.33) + pr_term(0.2) = 13.83, which is clipped to 2.0 by line 53. The docstring claim 'Bounded [0
  - **Fix:** Derive and state the exact saturation speedup: S_sat = (2.0 - CORRECT_FLOOR - PR_BONUS * max_pr) * P_TARGET = (2.0 - 0.3 - 0.2) * 1.5 = 2.25x. All correct kernels faster than 2.25x earn identical reward 2.0. Either (a) raise the cap to e.g. 4.0 or (b) use log-speedup: speedup_sco
- **[S4] BOOTSTRAP CREDIT CREATES A REWARD DISCONTINUITY AT step=anneal_steps THAT IS MATHEMATICALLY UNDEFINED AND PRODUCES INCORRECT ADVANTAGE SIGNS** — The bootstrap_credit function returns 0.0 when step >= anneal_steps (=40) and bonus*(1 - step/anneal_steps) otherwise. At step=39, credit = bonus*(1/40) > 0. At step=40, credit = 0.0. This is a step discontinuity, not a linear anneal — linear anneal would be zero at step=anneal_steps only if the for
  - **Fix:** Either (a) integrate bootstrap_credit as a named conditional branch inside compute_reward so it is subject to the same freeze and hash, with an explicit flag in reward_config.json to enable it; or (b) remove it entirely and use P_TARGET ladder / curriculum as the sole escalation.
- **[S4] THE L2 FLUSH BUFFER SIZE IS MATHEMATICALLY UNDERSIZED FOR H100: 64MB flush buffer does not cover the H100's 50MB L2 cache, and the flush protocol does not account for the eager baseline reusing the same buffer** — bench_core.py line 21 (verified): 'flush = torch.empty(int(64e6 // 4), dtype=torch.int, device="cuda")'. int(64e6//4) = 16,000,000 elements of int32 = 64MB. The H100 SXM5 has an L2 cache of 50MB. 64MB is larger than 50MB, so the flush buffer is theoretically sufficient. However, the buffer is alloca
  - **Fix:** Move flush.zero_() to BEFORE s.record(), not after: 'flush.zero_(); torch.cuda.synchronize(); s.record(); kernel_fn(*inp); e.record()'. This excludes flush overhead from the timed region. Verify with triton.testing.do_bench source (it calls 'cache.zero_()' before starting the eve
- **[S4] THE ANTI-HACK FORMAL BOUND FOR EXPLOIT 1 (ATen PASSTHROUGH) IS INCOMPLETE: torch.ops.aten.mm.default IS NOT CAUGHT BY BANNED_CALLS** — anti_hack.py (verified from source): BANNED_CALLS = {'torch.matmul', 'torch.bmm', 'torch.einsum', 'F.softmax', 'torch.compile', 'F.linear', 'F.conv2d', 'torch.nn.functional'}. The AST check for a call like 'torch.ops.aten.mm.default(a, b)' produces an Attribute node chain: torch -> ops -> aten -> mm
  - **Fix:** Add 'torch.ops' and 'ops.aten' to BANNED_CALLS (or more precisely, add a check in ast_clean that walks Attribute chains and rejects any chain containing 'ops' followed by 'aten'). Also add 'cublas', 'cudnn', 'cublasLt' to BANNED_NAMES. Implement _dotted() to handle chains of arbi
- **[S4] GRPO NaN ADVANTAGE WHEN std(r)=0: THE SPEC'S MITIGATION THRESHOLD std>0.05 IS NOT DERIVED AND THE trl IMPLEMENTATION DOES NOT GUARANTEE SAFE FALLBACK** — trl GRPOTrainer computes advantages as (rewards - mean) / (std + epsilon) where epsilon is a small constant. The spec's calibration Stage A requires std(R)>0.05 as a go/no-go gate. However: (1) The value 0.05 is stated as 'an engineering heuristic, not derived from a GRPO convergence/power analysis'
  - **Fix:** Add a per-step std-collapse detector in callbacks.py: if std(group_rewards) < 0.01 for three consecutive steps, checkpoint and abort (do not train on degenerate advantages). Separately, confirm trl's epsilon in the std-normalization path and set scale_rewards='group' explicitly i
- **[S4] THE DAVINCI PR TERM CONTRIBUTES ZERO GRPO GRADIENT AND THE SPEC ACKNOWLEDGES THIS BUT DOES NOT DERIVE THE CONSEQUENCE FOR TRAINING DYNAMICS** — The GAPS section for daVinci integration states: 'pr_frac is approximately constant (~1.0) for all genuine single-op kernels, so the +0.2 additive PR bonus is effectively a constant offset on correct rollouts and contributes ~0 gradient under GRPO group-normalization.' This is mathematically correct
  - **Fix:** Either (a) remove PR_BONUS entirely from compute_reward, simplify to reward = CORRECT_FLOOR + speedup_score clipped to 2.0, and save the 0.5h daVinci integration slot; or (b) make pr_frac actually variable by measuring it at multi-op scope rather than single-op scope (requires ru
- **[S4] FORMULA ERROR — speedup_score can exceed the stated reward bound [0, 2.0], making the bound claim false** — In rewards.py line 49: speedup_score = min(speedup, SPEEDUP_CAP) / P_TARGET. With SPEEDUP_CAP=20.0 and P_TARGET=1.5, the maximum speedup_score = 20.0/1.5 = 13.33. The formula then computes reward = CORRECT_FLOOR + speedup_score + pr_term = 0.3 + 13.33 + 0.2 = 13.83 before the min(reward, 2.0) cap on
  - **Fix:** Either set SPEEDUP_CAP to P_TARGET * (2.0 - CORRECT_FLOOR - PR_BONUS) = 1.5 * 1.5 = 2.25 so the pre-clip formula cannot exceed 2.0 at maximum PR, or normalize speedup_score = min(speedup/P_TARGET, 1.0) as done in the IMPLEMENTATION_COMPONENTS.md pseudocode (which correctly caps s
- **[S4] MISSING FORMULA — the delta-method CI for speedup is invoked but never derived; the 'money slide error bars' have no mathematical basis in the spec** — The Measurement Protocol section states that per-kernel speedup confidence intervals are computed via the 'delta method' and will appear as error bars on the money slide. The delta method for a ratio T_ref/T_kernel requires: Var(speedup) approx speedup^2 * (Var(T_ref)/T_ref^2 + Var(T_kernel)/T_kerne
  - **Fix:** Change bench_kernel to return the raw times list: add 'raw_times': times to the return dict. Add a CI function: def speedup_ci(times_kernel, times_ref, alpha=0.05): compute per-iteration ratios t_ref[i]/t_kernel[i] (matched-pair bootstrap, B=1000 resamples) -> return (lo, hi). St
- **[S4] ANTI-HACK GAP — torch.ops.aten.mm and cublas dispatch are not banned; Exploit T15 is unguarded in the actual code** — anti_hack.py BANNED_CALLS (confirmed from source, lines 6-8) contains: {'torch.matmul', 'torch.bmm', 'torch.einsum', 'F.softmax', 'torch.compile', 'F.linear', 'F.conv2d', 'torch.nn.functional'}. The AST-ban function _dotted(node.func) reconstructs dotted attribute chains. However, torch.ops.aten.mm.
  - **Fix:** Extend BANNED_CALLS to include 'torch.ops', 'torch.ops.aten', and any prefix starting with 'torch.ops'. Implement _dotted() to recursively resolve all attribute chains of arbitrary depth and check if any prefix of the chain appears in BANNED_CALLS. Add a specific ban on the strin
- **[S4] STATISTICAL ERROR — the generalization gap claim conflates the number of evaluation tasks with the number of independent observations** — The Generalization & Statistics section states n_test = 24 held-out tasks (4 ops x 6 test shapes). The MDD calculation uses a two-proportion z-test assuming 24 independent Bernoulli trials. However, each 'trial' is one shape evaluated on one op at one checkpoint. Across the 6 test shapes for a singl
  - **Fix:** Replace the independence assumption with an explicit intra-op correlation correction. Report MDD using n_eff = n_total / (1 + (m-1)*rho_hat) where rho_hat is estimated from the pilot calibration run. On the money slide, frame the result as 'directional, single run, n_eff approx 7
- **[S3] FORMULA DEVIATION FROM DAVINCI EQ1 IS MIS-CITED: Protean claims its reward is 'Eq1 continuity' and an additive-bonus port of daVinci Eq1, but daVinci Eq1 is R = C(y) * (1 + speedup + PR), a multiplicative structure where PR enters inside the C(y) gate. Protean's formula is reward = CORRECT_FLOOR + speedup_score + pr_term, which is additive-flat and not a port of Eq1 in any algebraic sense.** — daVinci paper Eq1 (verified from PDF p.3): R_{i,t} = C(y_{i,t}) * (1 + speedup_{i,t} + PR_{i,t}). This is a three-term sum multiplied by correctness. Protean's formula is: reward = 0.3 + min(speedup,20)/1.5 + 0.2*clip(pr,0,1). The structural differences are: (1) daVinci's PR enters as a speedup-comm
  - **Fix:** Remove all claims that the formula is a port of daVinci Eq1. State precisely: 'Protean uses a custom scalar reward reward = max(0, min(CORRECT_FLOOR + speedup/P_TARGET + PR_BONUS*pr, 2.0)) that is inspired by but algebraically distinct from daVinci Eq1. Key departures: (a) CORREC
- **[S3] GRPO SELF-INCLUSION BIAS CALCULATION IS WRONG: The spec claims 12.5% self-inclusion bias at N=8. The correct formula is 1/(N-1) = 1/7 = 14.3% for the GRPO mean baseline; the 12.5% figure (= 1/N = 1/8) is the wrong denominator and corresponds to a non-LOO mean, not the GRPO baseline.** — The GRPO advantage (DeepSeekMath arXiv:2402.03300, Eq.3) is A_i = (r_i - mean_{j in group}(r_j)) / std_{j in group}(r_j). The mean in the denominator includes sample i itself (group mean of all G=8 samples). The self-inclusion bias is therefore the fraction of the baseline that comes from sample i's
  - **Fix:** Correct the description: GRPO uses mean baseline (includes self, bias = 1/G = 12.5% at G=8, correct as stated). TRLOO uses LOO mean baseline (excludes self, bias = 0). These are orthogonal to whether std normalization is applied. Rewrite the comparison: 'GRPO group mean includes 
- **[S3] BENCH_CORE.PY MEDIAN COMPUTATION IS WRONG: times.sort() then times[len(times)//2] is the UPPER MEDIAN for even-length arrays, not the true median, and for odd-length arrays it is also offset by 1 from the standard definition** — bench_core.py line 31 (verified): 'times.sort(); t_kernel = times[len(times)//2]'. For reps=100 (verified from function signature), len(times)=100, len(times)//2 = 50. The true median of 100 values is the average of the 50th and 51st elements (0-indexed: elements [49] and [50]). The code returns tim
  - **Fix:** Replace 'times[len(times)//2]' with 'statistics.median(times)' (from the Python standard library) or equivalently '(times[n//2-1] + times[n//2])/2 if n%2==0 else times[n//2]' where n=len(times). Add a unit test asserting bench_kernel returns the same value as statistics.median on
- **[S3] DAVINCI ABLATION 1 IS MIS-APPLIED: The spec cites 'Ablation 1 (44.8->20.6%)' as justification for skill injection being load-bearing, but the actual numbers from daVinci Table 2 show the drop is from 44.8 to 16.2 (Fast1) at Level 2 for the 8B model, not 44.8->20.6** — From daVinci paper Table 2 (verified from PDF p.7-8): daVinci-kernel-8B Level 2 Fast1 = 44.8. Ablation 1 (no skill injection at inference) for 8B Level 2 Fast1 = 20.6. The spec correctly cites '44.8->20.6%' for the 8B Level 2 Fast1 threshold. However, the spec's claim that this ablation 'is the larg
  - **Fix:** Replace the Ablation 1 citation with the honest comparison: Protean's static BM25 skill prefix most closely matches daVinci Ablation 4 (policy RL + BM25 selection, no LLM rerank, no joint skill co-evolution). Ablation 4 at 8B Level 2 Fast1 shows 22.1% — competitive at low thresho
- **[S3] STATISTICAL POWER MATH IS INTERNALLY INCONSISTENT: THE SPEC STATES MDD ~27pp AT n=24 IN ONE SECTION AND ~20pp IN ANOTHER SECTION FOR THE SAME EXPERIMENTAL DESIGN** — The Generalization & Statistics section states: 'At n=24 held-out tasks the MDD at 80% power is ~27 pp; the held-out evaluation is genuinely underpowered for sub-20pp gaps.' The Task & Shape Sampling Design section states: 'with n_test=24 held-out evaluations the minimum detectable delta at 80% powe
  - **Fix:** Fix by providing a single MDD table keyed to the assumed baseline rate: MDD(p=0.1, n=24, power=0.8, alpha=0.05) = ?, MDD(p=0.2) = ?, MDD(p=0.5) = ?, using the standard two-proportion z-test formula. Pick the most conservative (p=0.5) as the stated MDD. The Wilson half-width is a 
- **[S3] BENCH_CORE.PY SEED_POST=44 IS A FIXED CONSTANT SHARED ACROSS ALL ROLLOUTS AND ALL STEPS, MAKING THE POST-TIMING CORRECTNESS CHECK NON-ADVERSARIALLY GAMEABLE BY A POLICY THAT LEARNS THE CONSTANT** — bench_core.py (verified): SEED_CORRECT=42, SEED_TIMED_BASE=43, SEED_POST=44. SEED_POST is a module-level constant, not a per-rollout variable. Every post-timing correctness check for every kernel, at every training step, for every shape, uses make_inputs(shape, dtype, seed=44). A policy that learns 
  - **Fix:** Replace SEED_POST=44 with a per-rollout derived constant: SEED_POST = hash(kernel_src_sha256 + str(shape) + str(step)) % (2**32), computed in the subprocess before the post-timing check. This makes SEED_POST unpredictable from the model's perspective. Alternatively, use SEED_POST
- **[S3] THROUGHPUT BUDGET IS BASED ON AN UNVERIFIED ESTIMATE WITH A 35x SPREAD: THE SPEC STATES 68-2127 STEPS OVERNIGHT, AND THE MONEY SLIDE'S STATISTICAL POWER DEPENDS ON REACHING THE UPPER END OF THAT RANGE** — The Throughput & Resource Budget section states: 'The 35x spread between optimistic (~2127 steps) and pessimistic (~68 steps) step counts means the 200-step target is a planning heuristic.' The spec also states: 'At n=24 held-out tasks the MDD at 80% power is ~27 pp... the held-out evaluation is gen
  - **Fix:** Derive the joint constraint: given MDD~27pp at n=24, n_test=6 shapes x 4 ops, and assuming the true gap (if any) is 15-20pp (optimistic for a 7B overnight run), the probability of observing a significant gap is below 50% even at 200 steps. State honestly: 'The money slide is dire
- **[S3] FORMULA ERROR — daVinci Eq2 is TRLOO not GRPO; the spec's 'GRPO self-inclusion bias' math is wrong** — Section 7 cites 'Shao et al., DeepSeekMath, arXiv:2402.03300' as the source for the GRPO objective. The daVinci paper (Eq2, confirmed from the PDF) uses TRLOO: A_{i,t} = G_{i,t} - (1/(N_t - 1)) * sum_{j != i} G_{j,t}. This is leave-one-out without std normalization. Standard trl GRPOTrainer (DeepSee
  - **Fix:** In §7, write two separate formulas. For trl GRPO (what Protean ships): A_i = (R_i - mean_G(R)) / std_G(R), and note the mean includes sample i giving a 1/G bias. For daVinci TRLOO (what Protean defers): A_{i,t} = G_{i,t} - (1/(N_t-1)) * sum_{j!=i} G_{j,t}, with 1/(G-1) LOO bias. 
- **[S3] FORMULA ERROR — the bootstrap credit can produce reward > CORRECT_FLOOR for an incorrect kernel, creating a correctness-inversion at step 0** — bootstrap_credit(compiles=True, imports_triton=True, step=0, anneal_steps=40) returns (0.1 + 0.2) * (1 - 0/40) = 0.3. compute_reward for an incorrect kernel returns reward=0.0 (hard zero, line 36 of rewards.py). The spec states that bootstrap_credit is 'added' to the compute_reward output, but the i
  - **Fix:** Specify the exact integration: bootstrap_credit must ONLY be added after the correctness hard-zero gate passes, or it must be gated itself on correct=True. Write the composite formula explicitly: reward_total = compute_reward(...)['reward'] + (bootstrap_credit(step) if correct el
- **[S3] TIMING BUG — fresh inputs per timed iteration make the median unreliable for fast kernels due to make_inputs CPU overhead being included in the CUDA event window** — bench_core.py lines 24-29: for each timed iteration, the code calls make_inputs(shape, dtype, seed=SEED_TIMED_BASE + i) (CPU), then flush.zero_() (GPU), then records start event, calls kernel_fn(*inp), records end event, then synchronizes. The CUDA events s.record() and e.record() bracket only the k
  - **Fix:** In bench_core.py, pre-allocate a pool of N+warmup input tuples on device before the timing loop: inp_pool = [make_inputs(shape, dtype, seed=SEED_TIMED_BASE+i) for i in range(warmup+reps)]. Then the timing loop indexes inp_pool[i] without any CPU allocation inside the event window
- **[S3] FORMULA INCONSISTENCY — the spec cites daVinci Ablation 1 as load-bearing (44.8% to 20.6% drop) but the numbers belong to Fast_1 Level 2 on a 14B model with full skill co-evolution, not to a static prefix on a 7B** — The daVinci Integration Math section states 'Ablation 1's skill-injection benefit (44.8%->20.6%) is load-bearing ONLY if the SFT warm-start is skill-conditioned'. From the daVinci PDF Table 2 (confirmed by direct read of pages 7-8), Ablation 1 (no skill injection at inference) for the 8B model shows
  - **Fix:** Correct the cited numbers to the 8B model (Level 2 Fast_1: 44.8% to 16.2%). Add a one-sentence caveat: 'This ablation applies to a model trained with full three-agent co-evolution; Protean's static-prefix benefit is unquantified and likely smaller.' Do not use this ablation as a 
- **[S3] GRPO OBJECTIVE ERROR — the spec states mu=1 (num_iterations) makes the PPO clip inactive but does not flag that this also makes the entropy regularization from DAPO clip-higher meaningless** — Section 7 notes that num_iterations (mu) is left at 1, making the PPO clip functionally inactive. This is correct. The spec then mentions 'clip-higher (eps_high=0.28) DAPO exploration benefit for rare Triton syntax tokens' as a stretch goal. DAPO's clip-higher (Xiong et al., arXiv:2503.14476) is spe
  - **Fix:** Remove clip-higher from the stretch goals entirely at mu=1, or add the prerequisite: 'clip-higher requires num_iterations >= 2 to produce any gradient effect; at mu=1 the importance ratio is identically 1 and the clip never binds.' If the team intends to implement clip-higher, se
- **[S2] REWARD FORMULA UNIT TEST ASSERTION IS ARITHMETICALLY WRONG FOR THE STATED PARAMETERS** — IMPLEMENTATION_PLAN.md section 4.1 states: 'tests/test_rewards.py asserts exact scalars: correct @ 1.0x speedup, pr=0.6 → 0.3 + 0 + 0.12 = 0.42'. Let us verify: speedup=1.0, SPEEDUP_FLOOR=1.1. Since 1.0 < 1.1, speedup_score = 0.0. pr_term = PR_BONUS * clip(pr_frac, 0, 1) = 0.2 * 0.6 = 0.12. reward =
  - **Fix:** Add unit test: assert compute_reward(correct=True, speedup=3.0, pr_frac=0.6, launches_timed=1, dtype_ok=True, shape_ok=True)['reward'] == 2.0 (hits clip). Add: assert compute_reward(correct=True, speedup=2.25, pr_frac=0.0, launches_timed=1, dtype_ok=True, shape_ok=True)['reward']
- **[S2] THE GRPO CLIP BOUND IS CITED AS DAPO'S eps_high=0.28 BUT trl GRPOConfig HAS NO eps_high PARAMETER IN ITS PUBLIC API — THE CITED STRETCH IS UNIMPLEMENTABLE WITHOUT MODIFYING trl INTERNALS** — Section 7 discusses 'clip-higher (eps_high=0.28) DAPO exploration benefit for rare Triton syntax tokens' and notes it is 'left as a noted stretch, unquantified for kernel generation.' However, trl's GRPOConfig (as of trl 0.9.x and the versions current through mid-2026) exposes only a single clip eps
  - **Fix:** Remove eps_high=0.28 from the training objective section entirely or explicitly gate it as 'NOT IMPLEMENTED, requires forking trl and patching GRPOTrainer._compute_loss to use separate upper/lower clip bounds.' Add explicit epsilon=0.2 to GRPOConfig in grpo_loop.py so the clip bo
- **[S2] CITATION ERROR — the spec cites 'Ben-David et al. 2010' for the covariate shift argument but the correct reference for domain adaptation PAC bounds in this context is Ben-David et al. 2010 IEEE TNNLS, not a generalization statement about kernel RL** — The Generalization & Statistics section cites 'Ben-David et al. 2010' as the source for 'disjointness is necessary but not sufficient for the generalization claim'. Ben-David et al. 2010 ('A theory of learning from different domains', Machine Learning 79:151-175) establishes that the target error is
  - **Fix:** Either remove the Ben-David citation and replace it with the direct observation ('disjoint train/test shapes are a necessary condition; we make no claim about sufficiency'), or compute the H-divergence proxy between S_train = {256,512,1024,2048} and S_test = {383,400,769,800,1600

## Round 3 — final attacks (completeness)

- **[S5] SAMPLER.py REFERENCES TRAIN_M/TEST_M WITHOUT IMPORTING THEM** — sampler.py line 12 references TRAIN_M and TEST_M as bare names in the function body of sample_task() ('pool = TRAIN_M if split == train else TEST_M') but the file contains no import statement importing those names from splits.py. The file imports only hashlib and random. Every call to sample_task() 
  - **Fix:** Add 'from protean.splits import TRAIN_M, TEST_M' as the third import line in sampler.py. Then add 'from protean.sampler import sample_task' and 'from protean.splits import _assert_split_disjoint' to manifest.py which references both without importing either. Verify with 'python -
- **[S4] GENERALIZATION CLAIM STATISTICAL UNDERPOWERING — THE SINGLE WEAKEST REMAINING PART** — The project's central scientific claim — a 'directional base-vs-trained reward gap on held-out continuous shapes' — rests on n=24 held-out evaluations (4 ops x 6 TEST_M shapes). At n=24 and an expected allclose rate near p=0.2, the Wilson half-width is approximately +/-16 percentage points and the m
  - **Fix:** Expand TEST_M from 6 shapes to 12 per op (add {192, 576, 960, 1344, 1729, 2561} — all off-grid, prime-adjacent or tiling-boundary, all disjoint from TRAIN_M by _assert_split_disjoint). This doubles n_test to 48 tasks, drops MDD to ~19 pp, and costs approximately $1 of H100 infere
- **[S4] GRPO GRADIENT COLLAPSE FROM ALL-ZERO-REWARD GROUPS IS UNMITIGATED BEFORE CALIBRATION RUNS** — GRPO requires reward variance within each group of G completions to produce a non-zero gradient. If all G=8 completions for a prompt produce reward=0 (the most likely outcome for a cold-start 7B on Triton kernel tasks without SFT warm-start, where allclose rate is near 0%), then std(r)=0, the normal
  - **Fix:** Add a per-step NaN guard in grpo_loop.py: after each optimizer step, assert that the mean group reward over the last step is finite ('assert not math.isnan(trainer.state.log_history[-1].get(rewards/mean, 0.0))'); on failure, checkpoint and abort with a clear error message rather 
- **[S3] PR_FRAC CONTRIBUTES ZERO GRPO GRADIENT — THE DAVINCI INTEGRATION IS DECORATIVE** — The daVinci paper's profiling-ratio term (Eq1: PR = T_generated / T_total) is designed for a multi-op multi-turn scope where the generated kernel is one of several GPU operations, so PR meaningfully varies between 0 and 1 and carries a real gradient. Protean's subprocess scope contains exactly one o
  - **Fix:** Do not overclaim the daVinci integration in the demo. The honest framing is already in the plan ('we integrated two daVinci techniques — the PR term as an additive anti-passthrough bonus and a static skill prefix'). Arm this framing explicitly on Slide 5: 'At single-op scope PR i
- **[S3] DELEGATES_TO_MATRIX_UNIT AST CHECK BANS tl.dot FOR MATMUL-CLASS OPS, BLOCKING LEGITIMATE TRITON KERNELS** — The anti_hack.py delegates_to_matrix_unit function returns True (exploit detected) if 'tl.dot' appears anywhere in the submitted source for matmul-class ops. In Triton, tl.dot is the canonical, correct, and intended way to perform matrix multiplication in a Triton kernel — it is the first thing any 
  - **Fix:** Replace the string-match 'tl.dot in src' with an AST-based check that flags tl.dot only when it appears as the sole operation in the kernel body with no surrounding tiled loop structure (i.e., the function body contains fewer than N_MIN=10 non-import, non-decorator statements). A
- **[S3] CLOCK-LOCK IS UNVERIFIED AND SILENTLY FAILS ON MODAL H100** — bench_core.py's @modal.enter() calls 'subprocess.run([nvidia-smi, -lgc, 1980], check=False)' with check=False, meaning a permission failure (which Modal H100 containers may not grant for clock locking via nvidia-smi -lgc) is silently swallowed. No assertion verifies that the clock was actually locke
  - **Fix:** After the subprocess.run nvidia-smi call, add a verification step: run 'nvidia-smi --query-gpu=clocks.gr --format=csv,noheader' and assert the returned value is within 5% of 1980 MHz. If the assertion fails, log a WARNING and set an environment variable CLOCK_UNLOCKED=1 that benc

## Completeness gaps (critic)


### Protean Completeness Critic — Prioritized Gap List (June 2026)

## Scope and Method

Every gap below was verified by directly reading the source stubs at `/Users/nihalnihalani/Desktop/Github/protean/src/protean/` and the train directory, then cross-checked against the IMPLEMENTATION_PLAN.md and IMPLEMENTATION_components.md prose, the daVinci-kernel PDF (pp 1-10), and live trl GRPO documentation (fetched from huggingface.co/docs/trl/main/en/grpo_trainer). Severity ratings: P0 = build-blocking crash before a single training step; P1 = incorrect output or silent wrong behavior; P2 = unimplemented feature with no workaround stated; P3 = undocumented assumption or statistical gap that will embarrass the demo.

---

## P0 — Build-Blocking Crashes (Nothing Runs Until Fixed)

### G1. shape_sampler.py raises ImportError at import; the moat invariant NEVER fires

**Verified file:** `/Users/nihalnihalani/Desktop/Github/protean/src/protean/shape_sampler.py` line 8-9.

The file contains:
```
from protean.splits import TRAIN_BANDS, HELD_OUT_BANDS, assert_disjoint
from protean.sampler import sample_shape
```

`splits.py` exports only `TRAIN_M`, `TEST_M`, `_assert_split_disjoint` (underscore-prefixed). None of `TRAIN_BANDS`, `HELD_OUT_BANDS`, or `assert_disjoint` exist. `sampler.py` exports `sample_task`, not `sample_shape`. Four name mismatches. Every module that imports `shape_sampler` (including `tasks.py` via its re-export chain) raises `ImportError` before executing a single line.

Additionally, `sampler.py` references `TRAIN_M` and `TEST_M` at lin

### Protean Completeness Critic: Verification & Evaluation Completeness — Prioritized Gap List with Fixes

## Scope and method

This report audits the completeness of the experiments, ablations, and metrics needed to credibly claim the Protean generalization result: that GRPO training on discrete train shapes improves kernel quality on disjoint held-out shapes. Every claim is grounded in the verified source files at `/Users/nihalnihalani/Desktop/Github/protean/src/protean/`, the daVinci-kernel PDF (verified pp. 1–10), and live trl/KernelBench/Triton docs fetched above. Verified facts are distinguished from CLAIMS.

Sources consulted:
- `/Users/nihalnihalani/Desktop/Github/protean/src/protean/rewards.py` (verified)
- `/Users/nihalnihalani/Desktop/Github/protean/src/protean/bench_core.py` (verified)
- `/Users/nihalnihalani/Desktop/Github/protean/src/protean/splits.py` (verified)
- `/Users/nihalnihalani/Desktop/Github/protean/src/protean/sampler.py` (verified)
- `/Users/nihalnihalani/Desktop/Github/protean/src/protean/shape_sampler.py` (verified)
- `/Users/nihalnihalani/Desktop/Github/protean/src/protean/manifest.py` (verified)
- `/Users/nihalnihalani/Desktop/Github/protean/src/protean/anti_hack.py` (verified)
- `/Users/nihalnihalani/Desktop/Github/protean/src/protean/launch_probe.py` (verified)
- `/Users/nihalnihalani/Desktop/Github/protean/src/protean/subprocess_runner.py` (verified)
- `/Users/nihalnihalani/Desktop/Github/protean/src/protean/task_catalog.py` (verified — only elementwise_add_relu is uncommented)
- daVinci-kernel arXiv 2606.16497, pp. 1–10 (verified: Eq1–Eq10, Table

### Protean Engineering Completeness Critique: Prioritized Gap List

## Methodology

All gaps below are grounded in direct file reads at the paths listed. Severity codes: SEVER-5 = build/runtime blocking (nothing works without fixing this), SEVER-4 = overnight-kick blocking (the training run cannot start), SEVER-3 = correctness/demo blocking (the money slide or red-team is wrong), SEVER-2 = operational hazard (silent failure, data loss, budget risk), SEVER-1 = hygiene / second-engineer confusion.

---

## TIER 1 — BUILD-BLOCKING (fixes required before `python -c 'import protean'` succeeds)

### GAP-1: shape_sampler.py imports four names that do not exist — guaranteed ImportError at first import [SEVER-5]

Verified: `/Users/nihalnihalani/Desktop/Github/protean/src/protean/shape_sampler.py` lines 8-12:
```
from protean.splits import TRAIN_BANDS, HELD_OUT_BANDS, assert_disjoint
from protean.sampler import sample_shape
assert_disjoint()
```
`/Users/nihalnihalani/Desktop/Github/protean/src/protean/splits.py` exports only `TRAIN_M`, `TEST_M`, `_assert_split_disjoint` (underscore-private). `/Users/nihalnihalani/Desktop/Github/protean/src/protean/sampler.py` exports only `sample_task`, not `sample_shape`. Four name mismatches; the module raises `ImportError` on import. The moat invariant `assert_disjoint()` never fires because the module never loads.

Fix (pick one convention and apply everywhere): In `splits.py` add three public aliases at module level — `TRAIN_BANDS = TRAIN_M`, `HELD_OUT_BANDS = TEST_M`, `assert_disjoint = _assert_split_disjoint`. 