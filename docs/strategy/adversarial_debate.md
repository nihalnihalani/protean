# Phase 14b — Full Adversarial Debate Transcript

> Complete record of every devil's-advocate attack and every defender rebuttal across all 3 rounds.
> 9 attack sets (3 finalists × 3 rounds) and 9 defense sets. Severity 1–5; residual_risk 1–5 (after fix).


---

# ROUND 1: R1 Landscape


## Protean — Round 1

**Devil's-advocate verdict:** fatal=True · Protean has two co-fatal attacks — the solo 24h critical path collapses if the ml-template pivot or GRPO wiring eats more than one 4-hour spiral, and the base-vs-trained delta at 7B is unproven and may not materialize overnight — meaning the single judging criterion (a real delta by 1 PM Sunday) is at serious risk of producing nothing to show, regardless of how clean the verifier design is on paper.

| # | Vector | Attack (severity) | Defender: holds? | Rebuttal / Fix (residual) |
|---|---|---|---|---|
| 1 | Time: No chance in 24h s | The critical path has at least six serial yak-shaving bottlenecks that each take 2-4 hours. The team must: (1) discover at hackathon start that ml-template is a torchtitan training scaffold with zero Triton/kernel primit (5) | True | This is the genuinely strongest attack and it partially lands — but the critical-path estimate is inflated because three of the six steps are already-solved, copy-pasteable assets in the cloned repos. Step 1 (template pivot) is NOT a discovery-at-the-venue risk: it is already resolved in the groundi (res 3) |
| 2 | RL: No/insufficient lear | The base model is Qwen2.5-Coder-7B. Kevin-32B's proven delta (correctness 56%->82%, speedup 0.53->1.10x) used a 32B model — 4.5x more parameters. Dr. Kernel's proven Fast@1.2 delta (5.6%->20.0%) used a Cold-Start-8B mode (5) | True | The reward-band / gradient-collapse risk is real and the 7B-vs-32B gap is correctly flagged, but the attack overstates the near-zero-start risk. The single most load-bearing fact is in the grounding: KernelBench L1 is single-op tasks where base frontier models land at ~18-31% fast_1, and crucially L (res 3) |
| 3 | Verifier: The reward is  | The daVinci-kernel paper (June 2026, published during the hackathon) identifies a profiling ratio exploit that the current Protean spec does not close: a kernel can be fast only on a tiny fraction of the operation's (3) | True | Each sub-exploit is real but each has a known, cheap, in-scope countermeasure documented in the grounding research, so this is a fixable verifier-hardening task, not a fatal flaw. The torch.*-wrapping exploit is closed by KernelGYM's Triton-launch instrumentation (drops hacking ~20%->~3%) which is o (res 2) |
| 4 | Researcher: It is not a  | A CTO judge from a frontier lab will immediately observe that Protean is KernelBench with a task generator bolted on. KernelBench already has 250 tasks, a fast_p metric, and a static held-out evaluation protocol. Th (4) | False | This conflates 'the research ideas are individually published' (true) with 'the artifact is not a contribution' (false), and the hackathon's judging criterion is a working RL environment with a clean verifier and a real delta — not paper-novelty. KernelBench is a STATIC 250-task eval harness with no (res 2) |
| 5 | YC: It is not a company | The startup thesis is 'sell the kernel-optimization RL environment to labs.' But labs (Anthropic, OpenAI, Google DeepMind) have already funded or built their own kernel optimization infrastructure: Cognition built Kevin- (3) | False | This is a pitch-narrative risk, not a technical kill, and it applies equally to every env-startup in the room (Idler/Mechanize face the identical 'why not build it yourself' question, yet the grounding shows labs are spending tens of millions and Idler claims a lab's largest-ever env contract). The  (res 2) |
| 6 | Demo: Nobody understands | The WOW moment is 'fast_1 rose from 18% to 40% on held-out shape range' on a slide. This requires the judge to understand what fast_1 means, what a held-out shape range is, what a Triton kernel is, and why beating PyTorc (4) | True | The live-fragility concern is legitimate and standard for any GPU demo — but it is mitigable to near-zero with pre-computed artifacts, and the grounding already shows speedups >1.5x are the AVERAGE (1.58x), so a >=2x hero task on at least one L1 op is well within the observed distribution, not a rar (res 2) |
| 7 | Competition: Someone doe | daVinci-kernel was published in June 2026 — during the hackathon weekend itself. If any competing team discovers this paper and presents its profiling-ratio reward, they immediately have a more defensible verifier than K (3) | False | Competition pressure is real but does not threaten survival — it threatens marginal differentiation, and the response is to absorb the competitor's edge rather than be beaten by it. If daVinci's PR reward is the threat, the fix to the verifier attack already ADOPTS daVinci's profiling-ratio term, ne (res 2) |
| 8 | Sponsor: Tool use is for | The Fireworks Training API (the primary GRPO training backend) is in 'private preview' — early access required, not GA. If the hackathon registration does not grant private-preview access, the team's GRPO training plan c (3) | False | None of these is load-bearing because each has a GA/in-toolchain fallback already identified in the grounding. Fireworks Training API being gated is fine: the plan's actual training path is trl/verl against a local vLLM rollout server on Modal (GA), with Fireworks used only as a GA fast-inference en (res 2) |

**Defender overall: survives=True**


## TESTBENCH-FORGE v2 — Round 1

**Devil's-advocate verdict:** fatal=True · KILL as a primary entry — the overnight GRPO signal is unproven (GRPO gradient collapse risk on sparse mutation-kill reward is documented and unmitigated), the solo 24h build requires 6+ non-trivial new components on top of an already-loaded Protean sprint, and the differentiator over MIST-RL (freshly-generated hidden mutants) is the exact piece that will not exist by demo time; if you cannot abandon it, FIX by: (1) run a reward-distribution smoke test on the base model in the first 2 hours to confirm median kill rate is above 5% before writing any GRPO code, (2) scope the mutation engine to a single operator (boundary-value swap only, known non-equivalent) to guarantee a continuous reward signal, and (3) present it as a second slide off Protean, not as a live demo, using pre-recorded kill-rate curves.

| # | Vector | Attack (severity) | Defender: holds? | Rebuttal / Fix (residual) |
|---|---|---|---|---|
| 1 | RL — No/insufficient lea | The mutation-kill reward is too sparse for GRPO to move in one overnight run. The coding-template's GradingRunner.run_tests() returns a binary 0.0/1.0 from pytest; TESTBENCH-FORGE upgrades this to a fraction (kills/mutan (5) | True | This is the strongest attack and partially lands — the precise delta numbers are unbacked extrapolation. But the collapse mechanism is over-stated and the named mitigation is concrete and cheap. (1) The reward is a continuous fraction killed/N, NOT binary — verilog-template/.../grade.py line 251 alr (res 3) |
| 2 | Verifier — The reward is | The phase13 spec says assert-False fails the reference gate, which is true. But there is a class of exploit the spec does not close: the agent writes a test suite that passes the reference but kills mutants by asserting  (4) | True | Two distinct claims here; one is real and one is over-stated. The side-effect/over-specification exploit is real but the existing template shows the defense pattern: verilog-template grade.py has a `hygiene_score()` (lines 205-221) that statically rejects suspicious patterns and requires structural  (res 2) |
| 3 | Time — No chance as a so | The phase13 build plan explicitly frames TESTBENCH-FORGE v2 as sharing '~80% of one codebase' with Protean and adds 'the mutation engine is the only new piece (~3h).' This is dangerously optimistic. The mutation  (5) | False | This attack is materially weakened by a fact it missed: a COMPLETE working mutation-kill testbench-writing grader already ships in the repos. verilog-template/tasks/stream_arb_fifo_cocotb_dv/donotaccess/grade.py is exactly 'agent writes a test suite -> harness runs it against a golden gate + a hidde (res 3) |
| 4 | Researcher — It is an ev | A legitimate RL environment has state, action, transition, and a reward that depends on the transition — the agent's action changes the world and the next state reflects that change. TESTBENCH-FORGE v2's two-yield patter (4) | True | Partially lands: the phase13 pseudocode IS single-turn (one prompt, one reward), so as written it is repeated sampling, not sequential credit assignment. But the attack overstates the implementation gap and the 'eval not environment' framing is wrong on the merits. (1) Single-turn verifiable-reward  (res 2) |
| 5 | Competition — MIST-RL pu | MIST-RL (arXiv 2603.01409, March 2026) uses GRPO with mutation-testing score as RL reward for test-suite generation, achieves 28.5% higher mutation score than CodeRM-8B, and is open-source. The phase13 spec acknowledges  (4) | True | Lands as a presentation risk, not a substance-killer. The novelty IS thin if reduced to 'GRPO + mutation reward' — that is MIST-RL. But three differentiators survive scrutiny and at least one is already-built proof: (1) The HARDWARE variant is not vaporware — verilog-template ships a WORKING cocotb  (res 3) |
| 6 | Demo — Nobody understand | Protean's killer moment is undeniable: a timer drops from 4.2ms to 2.0ms on screen. Judges who do not know GPU kernels still understand 'faster = better.' TESTBENCH-FORGE v2's equivalent is 'bug-kill meter 30%->5 (3) | True | Lands as a real demo-craft risk but is fully mitigable and not fatal. The 'four layers of context' problem dissolves with the right framing: the visceral one-liner 'we inverted the coding agent — instead of passing tests, it writes tests that catch bugs' is immediately graspable, and the visual can  (res 2) |
| 7 | YC — It is not a company | The startup path stated in phase13 is 'verification-as-a-service — produce the test suites labs need to trust their coding agents.' This is a market claim, not a market analysis. The actual buyer of mutation-testing-as-a (3) | True | Lands as the weakest commercial story of the three finalists, and the concession is warranted — but the attack mis-frames the buyer. The product is NOT 'mutation-testing-as-a-service' sold to QA teams; it is an RL ENVIRONMENT sold to labs that want verifier-robust, reward-hacking-resistant training  (res 3) |
| 8 | Sponsor — Daytona use is | The phase13 plan lists Daytona as a sponsor for 'isolated test sandboxes' at $100 budget. The phase1 Daytona research confirms: Daytona is CPU-only (no GPU), Alpha-status Python SDK (0.189.0), with an inconsistent packag (3) | False | This attack is correct on the facts and the conclusion is to DROP Daytona — which the defense fully accepts, making the attack a non-issue rather than a wound. Daytona is explicitly 'optional' in phase13 ('Daytona $100 optional'), so removing it costs nothing. More importantly, the correct isolation (res 1) |

**Defender overall: survives=True**


## SILICON-FORGE v2 — Round 1

**Devil's-advocate verdict:** fatal=False · SILICON-FORGE v2's primary cause of death is a compounding episode-latency trap: Yosys synth_ice40 runs 5-60 seconds, Verilator compile 90 seconds, and the synthesis reward is a binary band-pass (not a continuous PPA signal), meaning the overnight GRPO run produces too few episodes to show a real delta and the gate-count chart tells judges nothing about silicon quality — making this simultaneously infeasible in 24h as a solo build, undemonstrable to a non-EDA audience, and unable to show the PPA optimization signal its own pitch claims; VERDICT: FIX-THEN-PROCEED only if (a) a continuous normalized gate-count reward replaces the binary band-pass in grade.py, (b) at least three distinct design families with pre-written hidden testbenches are built before Saturday midnight, and (c) synthesis is parallelized across Modal workers to bring per-episode wall-clock below 45 seconds; absent all three fixes, treat this as a slide-only backup exactly as phase13 recommends.

| # | Vector | Attack (severity) | Defender: holds? | Rebuttal / Fix (residual) |
|---|---|---|---|---|
| 1 | RL / Learning Signal | Yosys synth_ice40 runs 5-60 seconds per episode. With a 90-second Verilator compile plus a 30-second sim timeout already baked into the repair grader (confirmed in stream_arb_fifo_repair/donotaccess/grade.py), each grade (5) | True | The latency math is partly inflated but the core risk is real and phase13 already concedes it ('delta is the risk'). Correction of the math: the 90s/60s/30s figures in grade.py are fail-closed TIMEOUTS (ceilings), not typical runtimes. The run() helper (lines 73-109) only hits them on a hung/patholo (res 3) |
| 2 | Verifier / Reward Gameab | The repair grader's synthesis score is gated entirely on synth_ice40 succeeding and producing a cell count in [60, 1400]. That band is extremely wide — 1400/60 = 23x. An agent that submits a functionally correct but grot (4) | True | This attack is the most technically correct AND the most fixable, but it materially overstates the weight. The grader (lines 215-218) splits synthesis into THREE binary 0.10 terms: success, latch_free, gate_in_band. gate_in_band is only 0.10 of total reward — NOT the 0.30 the attack repeatedly impli (res 2) |
| 3 | Researcher / Not a Real  | The verilog-template ships exactly three tasks: stream_arb_fifo_repair, stream_arb_fifo_cocotb_dv, and stream_arb_fifo_formal — confirmed in task_catalog.py. All three tasks train on the same single RTL module (stream_ar (4) | True | Concede fully: this is the strongest attack and it is correct. task_catalog.py (verified) hardcodes exactly three TaskSpecs all with module='stream_arb_fifo', and there are 5 hardcoded mutant .sv files per track. A genuine RL ENVIRONMENT (vs an eval) needs task diversity, and writing valid hidden te (res 4) |
| 4 | Competition / Someone Do | Alpha-RTL (arXiv 2606.05253, published June 2026, during the hackathon window) is near-identical to the SILICON-FORGE v2 reward design: composite reward r = 0.1*r_syn + 1.0*r_func + 10.0*r_ppa, using Yosys + OpenSTA with (3) | False | The differentiator is real and one sentence, not 'too many words.' Alpha-RTL and ChipSeek are TRAINING RECIPES that each built bespoke, unreleased infrastructure; neither is a reusable, runnable RL ENVIRONMENT with a hidden, anti-gamed verifier you can point a policy at today. The HUD verilog-templa (res 2) |
| 5 | Demo / Nobody Understand | The proposed demo moment is 'timing: MET + gate-count chart flips green; an AI learned to write working silicon.' This requires the judges to know what gate count means, why a lower cell count after synthesis is an impro (3) | False | The legibility gap is real relative to Protean's '4.2ms->2.0ms', but the demo failure is a presentation choice, not an environment property. The universally legible framing is functional: 'the AI's testbench MET timing and PASSED the hidden bug-suite it had never seen — 0/4 scenarios passing at (res 2) |
| 6 | YC / Not a Company | The startup path slide says 'the on-ramp to AI chip design; EDA incumbents + silicon startups want it.' But the actual paying customers for an RL environment are frontier labs (Anthropic, OpenAI, Google DeepMind), not ED (3) | True | The attack correctly skewers the slide's stated customer (EDA incumbents/silicon startups won't buy an OSS-CAD RL harness) and is right that the buyer is frontier labs. But it overreaches on TAM-is-zero. The same 'labs already have corpora and custom flows' argument applies verbatim to coding enviro (res 3) |
| 7 | Sponsor / Tool Use is Fo | The sponsors listed for SILICON-FORGE v2 are HUD (load-bearing for the env harness) and Modal/GCP for synthesis compute. Modal is listed as load-bearing for GPU bench in Protean but SILICON-FORGE uses it only for CP (2) | True | Largely conceded: SILICON-FORGE genuinely cannot use Modal's marquee GPU capability, and the Modal-snapshot-speedup argument is weak for a static OSS-CAD binary. But the attack frames sponsor integration too narrowly. The load-bearing, non-trivial Modal use here is horizontal FAN-OUT of CPU synthesi (res 3) |
| 8 | Time / No Chance in 24h  | SILICON-FORGE v2 is explicitly ranked third and conditionally built only if Protean shows signal by Saturday evening. The verilog-template task catalog is hardcoded to one module with fixed hidden testbenches and fi (4) | True | Concede the headline: as a from-scratch multi-family build, this does not fit 24h solo with Protean as primary, and phase13 already correctly demotes it to conditional/slide-only insurance (lines 95-96, 115). The attack's effort estimate is accurate for the multi-family version it assumes. The  (res 4) |

**Defender overall: survives=True**


---

# ROUND 2: R2 Finalists


## Protean — Round 2

**Devil's-advocate verdict:** fatal=True · KILL — two independently fatal flaws: the ml-template fork assumption is factually wrong (it is torchtitan, not a kernel harness), burning 4-6 hours that do not exist, while the reward formula as written is directly exploitable by the torch-wrap hack that Kevin documented specifically for 7B-class models, meaning an un-guarded overnight run produces a fraudulent reward curve, not a learning signal. Fix both before committing to this project or the demo collapses on two fronts simultaneously.

| # | Vector | Attack (severity) | Defender: holds? | Rebuttal / Fix (residual) |
|---|---|---|---|---|
| 1 | TIME / FEASIBILITY | The ml-template fork assumption is false and costs 4-6 hours that do not exist in the schedule. The phase0 doc states 'ml-template already ships GPU scaffolding → faster start.' After reading ml-template/env.py line 1 th (5) | True | The factual core is verified and conceded: ml-template/env.py line 1 is 'ML training environment -- torchtitan experiments' with scenarios train_to_target/audit_training_data/etc., and a grep across all three starters finds ZERO kernel primitives (no do_bench, allclose-on-kernels, JITFunction instru (res 4) |
| 2 | VERIFIER / REWARD HACKIN | The reward formula as written in phase13 — r = 0 if not allclose else min(speedup/P, 1.0) — is directly exploitable by the exact attack Kevin documented for 7B models, and the exploit earns full reward of 1.0. The attack (5) | True | Conceded as the most important attack, and it is correct that the bare formula plus a P_TARGET of 1.0 yields r=1.0 for a PyTorch passthrough. This is the single largest correctness risk and the research (Kevin, Dr. Kernel ~20%->3%) confirms it is the dominant 7B failure mode. BUT it is also the most (res 2) |
| 3 | RL / LEARNING SIGNAL | The 18%→40% fast_p delta claim is not grounded in any published result and has two structural problems that make it unreachable in 5 hours of GRPO. First, the 18% starting score implicitly requires a successful 2-hour SF (4) | True | The specific number 18%->40% on held-out in 5h is not defensible and should be dropped; conceded. Two true sub-points: a raw Qwen2.5-Coder-7B will start near 1-5% fast@1 on hard ops, and JIT cold-start throttles step count. The honest rebuttal reframes the demo claim rather than defending the number (res 4) |
| 4 | RESEARCHER / NOT A REAL  | A CTO judge who knows KernelBench will immediately ask: what is the difference between this environment and running KernelBench eval in a loop? The honest answer, absent the op-graph sampler being built and the held-out  (4) | True | Holds conditionally — it is true IF the team ships only static tasks, and false IF the descoped shape-sampler + documented train/test split lands. The genuinely defensible novelty (confirmed against the 2025-26 literature in the research notes) is the held-out-shape generalization VERIFIER: KernelBe (res 3) |
| 5 | YC / NOT A COMPANY | The startup path stated in phase13 is 'sell the kernel-optimization RL environment to labs.' The investor kill shot: who pays, how much, and why not just use KernelBench? The HUD Series A thesis — 50+ businesses build en (3) | True | Largely holds as a business critique and a 24h hackathon cannot manufacture an ACV or a signed customer. The honest rebuttal is that this is the SAME market-structure objection that applies to EVERY env-as-product entry under the HUD Series A thesis (a handful of technically capable lab buyers) — so (res 3) |
| 6 | COMPETITION / ME-TOO | DRTriton (March 2026) already ships a Qwen2.5-Coder-7B Triton kernel RL pipeline with CSP-DAG op-graph sampling, a 4-level difficulty curriculum, and a KernelBench Level 2 result of 92% faster-than-PyTorch. That is the s (3) | False | Does not hold as fatal. The attack itself concedes the differentiator (reusable-env packaging + the held-out shape verifier) and then asserts it 'isn't demonstrable in 24h' — but a working HUD two-yield environment with a train/test shape split and a generalization curve IS demonstrable in 24h under (res 2) |
| 7 | DEMO / COMPREHENSION | The 'WOW moment' in phase13 is a live 4.2ms→2.0ms kernel speedup on a GPU in the room. This demo has three independent failure modes that compound. First, CUDA-event timing on a conference-hall GPU (likely an H100 or A10 (3) | False | Holds in part (the live-WiFi timing risk and the comprehension gap are real) but NOT fatal, and the strongest sub-claim — that a pre-recorded fallback undermines the verifiability thesis — is wrong. The pitch is 'the VERIFIER cannot be fooled by the model,' i.e. non-gameability of the reward against (res 2) |
| 8 | SPONSOR / TOOL USE FORCE | The phase13 sponsor section lists Modal as 'non-core: parallel compile+bench plane' and HUD as the load-bearing sponsor. Modal's $250 credit is the largest single credit at the hackathon ($250 vs HUD $200 vs Daytona $100 (2) | False | Does not hold as a real risk. Modal usage is actually CORE and well-justified here, not weak: GPU kernel RL requires real GPUs for both rollout generation and the compile+bench plane, which is exactly Modal's serverless-GPU value prop — Protean is one of the few project types that genuinely nee (res 1) |

**Defender overall: survives=False**


## TESTBENCH-FORGE v2 — Round 2

**Devil's-advocate verdict:** fatal=True · KILL: The grading wall-clock time (25s/mutant, no timeout in coding-template runner.py) makes a real base-vs-trained delta by 1PM Sunday structurally impossible at any model size, and the remaining attacks — a gameable total-kill-rate reward, an env that is functionally an eval wrapper, and a startup narrative with no defensible moat over free OSS tooling — leave nothing standing even if the throughput problem were solved.

| # | Vector | Attack (severity) | Defender: holds? | Rebuttal / Fix (residual) |
|---|---|---|---|---|
| 1 | RL: No Training Delta in | The grading wall-clock time makes overnight GRPO training nearly impossible at a useful episode rate, and the expected 30%->58% delta is fabricated with no published basis. (5) | True | The two factual claims are correct and verified: coding-template/grading/runner.py runs subprocess.run with NO timeout (lines 110-116, 149-154), and 30%->58% has no published basis. CONCEDE both. But the throughput math is overstated by conflating two different test stacks. The 25s/mutant figure is  (res 3) |
| 2 | Verifier: Reward Is Game | The over-specification gate does not actually prevent a model from earning high reward with semantically vacuous test suites. (4) | False | The attack mostly defeats itself: it admits 'The score in that case is 0.' assert True kills zero mutants, so kills/total = 0, so reward = 0. There is no positive gradient toward vacuous suites. The residual concern - the model stacking 50 syntactically-distinct copies of the same effective assertio (res 2) |
| 3 | Researcher: It Is an Eva | The distinction between 'environment' and 'evaluation benchmark' collapses under inspection because the state space is trivially small and the action structure is borrowed wholesale from the coding-template without modif (4) | True | Partial concede. It is true a single fixed module has finitely many AST mutations, and TESTBENCH-FORGE will never match Protean's combinatorially-unbounded op-graph sampler on raw task count. But the attack frames task count over ONE module; the real task axis is (module x mutant-subset x refer (res 3) |
| 4 | YC: It Is a Feature, Not | The 'verification-as-a-service' startup narrative collapses because GitHub Actions already does mutation testing, and the buyable asset (the trained model) is commoditized within weeks of any frontier release. (4) | True | This lands and is largely a pitch/positioning problem, not a technical defect, so I concede it as the weakest part of the submission's business framing. The technical rebuttal: the grader being ~150 lines is true of EVERY env in this hackathon (coding-template grade is ~160 lines, verilog grade ~330 (res 4) |
| 5 | Time: 24h Solo Build Is  | The mutation engine alone — integrating mutmut 3.6.0 (new CLI format, no importable API), parallelizing grading, pre-filtering equivalent mutants, and wiring the HUD two-yield pattern — is a 3-4 day engineering task, not (4) | True | Concede the 3h estimate is false; the honest figure is closer to the attack's 10-16h. But the attack overstates the from-scratch burden by ignoring two accelerants. First, the entire scaffolding it lists as risky - two-yield wiring, hidden-answer-key isolation, fail-closed timeout, mutant-kill float (res 3) |
| 6 | Demo: Judges Will Not Un | The 'bug-kill meter 30%->58%' demo slide communicates nothing visceral to a non-specialist audience, and the 'this bug slipped past base, trained caught it' diff requires 2 minutes of explanation that a 4-minute hackatho (3) | True | Concede mutation score is less viscerally legible than a wall-clock speedup. But the WOW is recoverable with the right framing: do NOT show an abstract score delta - show a concrete narrated diff. 'Here is a real bug (an off-by-one injected into this function). The BASE model's test suite passes - i (res 2) |
| 7 | Sponsor: Moss / HUD Is N | TESTBENCH-FORGE can be built entirely without HUD or any hackathon sponsor, which collapses the premise of building for this specific hackathon. (3) | False | This proves too much - by the same logic NO env at this hackathon needs HUD (you could write any RL loop in raw veRL), so it is not a TESTBENCH-FORGE-specific weakness. More importantly it is factually weak: the submission inherits load-bearing HUD machinery it would otherwise rebuild - the @env.tem (res 2) |
| 8 | Competition: MIST-RL Is  | MIST-RL (March 2026, arXiv 2603.01409) already demonstrates mutation-kill RL with GRPO achieving +28.5% mutation score improvement, making TESTBENCH-FORGE a hackathon reproduction of a published paper without the key dif (3) | False | MIST-RL is a paper that trains a model; it is not a released, reusable RL ENVIRONMENT - the same env-vs-artifact distinction that the whole hackathon rewards. The three differentiators survive: (1) MIST-RL uses STATIC pre-generated mutants; TESTBENCH-FORGE evaluates on FRESH per-episode/held-out mut (res 2) |

**Defender overall: survives=True**


## SILICON-FORGE v2 — Round 2

**Devil's-advocate verdict:** fatal=True · SILICON-FORGE v2 dies on the grading math: 10 GRPO steps in 5 hours on a cold 7B model against a 210-second-per-episode grader cannot produce a statistically visible base-vs-trained delta by 1 PM, and without that delta the entire hackathon premise collapses — the plan's own authors already called it a slide-only insurance play, which is the correct verdict.

| # | Vector | Attack (severity) | Defender: holds? | Rebuttal / Fix (residual) |
|---|---|---|---|---|
| 1 | RL: No learning signal i | The cocotb DV grader runs 6 full cocotb+Verilator subprocess chains per episode (golden + 5 mutants), each with a 180-second timeout. Worst-case episode time: 1,080 seconds — 18 minutes. The formal grader is worse: 7 sby (5) | True | The attack is right about cocotb (6 runs x 180s) and formal (7 runs x 180s) — those tracks are training-poison and I concede them. But it conflates them with the track SILICON-FORGE should actually train on. Confirmed in stream_arb_fifo_repair/donotaccess/grade.py: the repair functional check runs a (res 3) |
| 2 | Demo: Nobody in a YC roo | The claimed demo moment is 'timing: MET + gate-count chart flips green' and 'an AI learned to write working silicon.' This is legible to EDA engineers, of which there are approximately zero in a YC partner room. Unlike K (4) | True | Partially holds and I concede the framing risk. 'AI learns to write working silicon from scratch' is a stretch for a repair-track demo and a judge will puncture it — the honest claim is 'AI learns to repair/complete RTL until a hidden coverage-gated testbench passes.' But the legibility attack overs (res 3) |
| 3 | Verifier: The reward is  | The cocotb DV grader's coverage subscore (0.20 weight) relies on a 'coverage.json' file written by the agent-authored testbench via a mark_coverage() helper. The hygiene check (line 214 of cocotb grade.py) verifies the s (4) | True | The core mechanism is REAL and confirmed: grade.py line 'coverage_data = golden.get("coverage")' reads a JSON the agent's own testbench writes via mark_coverage() (test_stream_arb_fifo.py line 20), and hygiene_score (line 213) only counts the string pattern occurrences, not behavioral truth. An agen (res 2) |
| 4 | Researcher/CTO: It is an | The verilog-template ships exactly three tasks: stream_arb_fifo_repair, stream_arb_fifo_cocotb_dv, stream_arb_fifo_formal — all three are variants of one module, stream_arb_fifo. The phase13 spec claims 'spec templates p (4) | True | Factually correct and conceded: task_catalog.py TASK_SPECS has exactly 3 entries, all variants of stream_arb_fifo, and no parameterization/template-expansion code exists anywhere in the repo. The phase13 claim 'spec templates parameterized -> infinite tasks' has no corresponding code. This is the mo (res 3) |
| 5 | YC investor: It is not a | The startup pitch is 'the on-ramp to AI chip design; EDA incumbents + silicon startups want it.' The actual paying customer set for a Verilog RTL RL environment is: chip design teams at large semiconductor companies (NVI (3) | True | Largely holds as a relative-ranking point and I concede SILICON-FORGE has the weakest near-term distribution of the three finalists. But the framing of the buyer is wrong: the customer is NOT semiconductor tapeout teams buying a production EDA replacement — it is AI LABS buying RL environments (HUD' (res 3) |
| 6 | Time: The 24h critical p | The verilog-template requires Verilator, Yosys, SymbiYosys, cocotb, and the oss-cad-suite bundle to be present in the deployed image. The grade.py files check for ~/utils/oss-cad-suite/bin on the PATH. Setting up and val (4) | True | Partially holds and I concede the cocotb integration risk and the self-described 'insurance' hedge. But the timeline assumes work the recommended scope eliminates. The repo SHIPS a working oss-cad-suite-based toolchain with the grader scripts, Dockerfile.hud, and uv.lock already wired — the install  (res 3) |
| 7 | Competition: The academi | VeriReason, VeriRL, RTLSeek, ChipSeek, Alpha-RTL, StepPRM-RTL, and PRO-V-R1 all published in 2025-2026 on RL for Verilog RTL generation. A CTO judge in this space will ask: 'How is this different from ChipSeek-R1 or Alph (3) | False | Overstated. Those are PAPERS that train MODELS using commercial EDA (Synopsys/Cadence) and large clusters; none release a reusable, open, deployable RL ENVIRONMENT with a non-gameable hidden-testbench verifier on a fully open toolchain. The differentiator is the same env-as-product framing that make (res 2) |
| 8 | Sponsor: HUD is load-bea | The phase13 spec lists HUD as load-bearing and Modal/GCP as compute for 'synth runners.' But the HUD rollout model sends one episode to one environment worker and waits for the EvaluationResult before dispatching the nex (2) | False | This is the weakest attack (the author rated it severity 2) and it does not hold under the recommended scope. It assumes the slow cocotb/formal chains; on the repair track at realistic ~15-30s/episode the grader is NOT the dominant bottleneck. More fundamentally, HUD's model is many parallel environ (res 2) |

**Defender overall: survives=True**


---

# ROUND 3: R3 Competitive


## Protean — Round 3

**Devil's-advocate verdict:** fatal=False · FIX-THEN-PROCEED: The RL delta timeline is the true kill condition — if Qwen2.5-Coder-7B produces all-zero reward batches on Triton tasks for the first 4+ hours of Saturday, the overnight GRPO run kicks with zero warmup and the 1 PM Sunday curve is flat; patch this by pre-collecting 50-100 Kevin-32B SFT examples Saturday afternoon (2-3h on one A100) to guarantee a non-zero compilable rate before GRPO starts, add the one-line dtype assertion to close the SOL-ExecBench precision-downgrade exploit, implement a genuine K-turn feedback loop (not just two-yield) to survive the researcher judge's 'this is just an eval' challenge, and verify KernelGYM's license on-site within the first 30 minutes — if it is MIT/Apache, fork it instead of building from hud-blank and recover the 4-6h schedule buffer.

| # | Vector | Attack (severity) | Defender: holds? | Rebuttal / Fix (residual) |
|---|---|---|---|---|
| 1 | RL — No visible delta in | The base model Qwen2.5-Coder-7B will produce nearly all-zero reward batches on Triton tasks in the first hours of training, meaning GRPO generates zero gradient and the training curve stays flat until — and possibly thro (5) | True | This attack lands the hardest and the spec's own '#single thing that wins or loses it' line concedes it: the live held-out delta by 1PM is THE win condition, and a flat curve is a negative result. The numbers are real — TritonForge's 22 min/episode (arXiv:2512.09196) means group=16 is ~352 GPU-min j (res 3) |
| 2 | Verifier — Concrete rewa | The reward formula is r = 0 if not allclose(out, eager, N random inputs) else min(speedup_vs_eager / P, 1.0). The allclose check uses default tolerances (rtol=1e-5, atol=1e-8 in PyTorch). A model that learns to cast the  (4) | True | Technically correct and confirmed by SOL-ExecBench (arXiv:2603.19173): precision downgrade is the single largest reward-hack category at 6.4%, and the spec's reward line lists 'allclose + post-timing functional re-check' but does NOT assert output dtype. An agent casting fp32->bf16 internally and up (res 1) |
| 3 | Researcher / Eval — This | The structural definition of an RL environment requires: persistent state that evolves over turns, actions that modify that state, and a reward that depends on the state trajectory. Protean's two-yield pseudocode in (4) | True | Partially holds and is the most credible *judge-facing* risk. The phase13 two-yield pseudocode (yield prompt -> grade -> yield reward) is literally single-turn, and a researcher who knows KernelGYM (multi-turn, open) or Kevin (K-turn compile-error feedback) will correctly say the pseudocode as writt (res 2) |
| 4 | Competition — KernelGYM  | KernelGYM (HKUST-NLP, github.com/hkust-nlp/KernelGYM) is a publicly open-sourced, distributed GPU RL environment for Triton kernel generation. The research context states explicitly that 'a knowledgeable competitor can f (4) | True | Real and unfalsifiable in the intel vacuum (no confirmed competitors, but KernelGYM and AMD Apex are both live open artifacts a GPU MODE alum could wrap). The attack is correct that a competitor starting from working multi-turn code could ship faster than Protean building the GPU sandbox from h (res 3) |
| 5 | Time — GPU sandbox from  | The ml-template is confirmed to be a torchtitan training template with no kernel optimization tasks. The research context and the grounding file both state this explicitly, and the ml-template/env.py source confirms it:  (4) | True | Holds, and it is the second-most-dangerous attack after the delta risk because it compounds: a 6PM-green sandbox leaves no slack for calibration + GRPO wiring before the 8AM kick. Confirmed: ml-template IS torchtitan (env.py scenarios are train_to_target/repair_degraded_recipe/audit_training_data, N (res 3) |
| 6 | Demo — Nobody in the roo | The phase13 spec describes the WOW moment as 'live hero kernel 4.2→2.0 ms; money slide fast_p 18%→40% on held-out.' Both of these require the audience to understand (a) what Triton is, (b) what PyTorch eager mode is, (c) (3) | False | Largely does not hold for THIS track, though the FastKernels baseline point inside it is valid. The 'live hero kernel 4.2->2.0 ms' is actually the strongest WOW primitive in the whole event: a stopwatch is the one verifier a generalist investor judge instantly trusts ('it ran twice as fast, on shape (res 2) |
| 7 | YC / Investor — This is  | The startup pitch is 'sell the kernel-optimization RL environment to labs.' The research context identifies this as the HUD Series A thesis: 'envs sold to labs.' But the environment being sold is a Triton kernel optimiza (3) | False | Does not hold as a kill, though it correctly narrows the buyer story. The premise that 'labs build in-house so no market' is contradicted by the event's own validating fact (phase0): HUD raised a Series A precisely on '50+ businesses build RL environments and SELL them to AI labs' — the env-is-the-c (res 2) |
| 8 | Sponsor — Modal is label | The phase13 spec's sponsor table reads: 'Sponsors (load-bearing): HUD (env/reward/GRPO) · Modal (non-core: parallel compile+bench plane).' Modal is explicitly labeled non-core by the team's own documentation. Yet Modal i (2) | False | Does not hold; it is a labeling nit, not a structural problem. The 'non-core' label means non-core to the ENV CONTRACT (HUD owns env/reward/GRPO), not non-core to execution. Modal providing the only GPU plane makes it deeply LOAD-BEARING, which is the opposite of the 'decorative sponsor' failure mod (res 1) |

**Defender overall: survives=True**


## TESTBENCH-FORGE v2 — Round 3

**Devil's-advocate verdict:** fatal=True · KILL on RL throughput alone: the mutation-kill reward loop produces approximately 18 GRPO steps overnight, which is provably insufficient for a visible base-vs-trained delta by 1 PM Sunday, and the three claimed differentiators (multi-turn, fresh mutants per episode, over-specification penalty) structurally undermine each other — fixing the throughput problem (pre-bake a fixed mutant pool) directly destroys the novelty claim (fresh per-episode mutants), while the multi-turn interaction that distinguishes this from MIST-RL cannot be built from the single-yield coding-template in the remaining hours.

| # | Vector | Attack (severity) | Defender: holds? | Rebuttal / Fix (residual) |
|---|---|---|---|---|
| 1 | RL: No learning signal / | The mutation-kill reward loop is too slow to produce a visible GRPO delta by 1 PM Sunday. The risk register explicitly identifies this as a 'quantitative showstopper': if a test suite takes 2 seconds to run and there are (5) | True | The arithmetic is structurally correct and I concede the default coding-template grading is serial: runner.py does shutil.copytree + git apply + one pytest subprocess PER grade() call (lines 77-128), so 50 mutants naively = 50 copytree+pytest cycles. This is the single most credible attack. BUT the  (res 3) |
| 2 | Verifier: the reward is  | The over-specification penalty (suite must pass N equivalent reference variants) is the only guard against brittle tests. The concrete unpatched exploit is: write a test suite that calls the function once with hardcoded  (4) | False | This attack misreads the reward as additive when it is multiplicative-and-gated. The exploit `assert(f(2)==4)` does NOT earn nonzero reward for being useless — it earns EXACTLY its mutation-kill fraction, which for a coarse single-point assertion is near-zero by construction. The reward IS kills/n_m (res 2) |
| 3 | Researcher kill shot: it | A CTO judge will immediately ask: what is the state transition between turns? In the two-yield pseudocode from phase-13, the agent receives a prompt, writes a test suite, and the episode ends. There is no turn-2 where th (4) | True | I concede the strongest version: I verified EVERY starter (coding env.py L154-155, verilog env.py L86/L93, ml env.py all scenarios) uses the identical two-yield pattern with NO intermediate yield. There is no template precedent for an env-level state transition that feeds structured kill-rate back t (res 3) |
| 4 | YC investor kill shot: f | The startup pitch is 'verification-as-a-service — produce the test suites labs need to trust their coding agents.' The problem is that both named buyers (Qodo at 50M Series A with 1M users, Diffblue Cover at 30K/year ent (4) | True | Partially holds and I concede the framing weakness, but the 'feature not a company' verdict overreaches. Qodo and Diffblue sell test GENERATION products to end-developers; TESTBENCH-FORGE is an RL training ENVIRONMENT whose buyer is whoever trains the model underneath — and the research itself shows (res 3) |
| 5 | Demo: nobody understands | The demo WOW moment is a 'bug-kill meter 30 percent to 58 percent' and a diff showing 'this bug slipped past base, trained caught it.' The comprehension problem: the judges must understand (a) what a mutant is, (b) what  (3) | False | The comprehension burden is real but the attack overstates it by listing five concepts; the demo only needs ONE visceral frame that every engineer already has: 'a bug the base model's tests let through, the trained model's tests catch.' You do not need to explain the word 'mutant' — you show a one-l (res 2) |
| 6 | Time: no chance in 24h s | The phase-13 spec says TESTBENCH-FORGE 'shares 80 percent of the codebase with Protean and the mutation engine is the only new piece taking 3 hours.' This arithmetic is wrong when examined against the actual coding- (4) | True | The '80% shared / mutation engine is the only new piece at 3h' claim is too optimistic and I concede it. The real shared portion (HUD SDK wiring, two-yield skeleton, uid-wall sandbox, GRPO Taskset.run plumbing) IS genuinely reusable from Protean and from coding-template, but the domain logic is (res 4) |
| 7 | Competition: MIST-RL and | The research confirms MIST-RL (arxiv 2603.01409, March 2026) is a published single-turn mutation-RL training recipe for test generation. GRPO-SMu (arxiv 2601.07593, January 2026, accepted at IEEE) combines hardware state (3) | False | The cannibalization argument rests on a false equivalence the attack itself introduces: it assumes 'pre-baked pool for throughput' == 'fixed pool, killing the fresh-per-episode claim.' My fix (attack #1) pre-bakes the pool PER TASK at task-build time, but tasks are sampled from an infinite module x  (res 2) |
| 8 | Sponsor: HUD tool use is | The Daytona credit ($100) is listed as 'optional for isolated test sandboxes.' The coding-template already provides a uid-wall sandbox via setpriv, which is exactly what TESTBENCH-FORGE needs for mutant execution isolati (2) | True | Largely holds and is the least important attack (severity 2, and I agree). Daytona IS redundant given the coding-template's setpriv uid-wall (env.py L49-62) — I will not pretend otherwise. The honest answer is that sponsor-integration is a tie-breaker prize, not the win condition; the spec already l (res 2) |

**Defender overall: survives=True**


## SILICON-FORGE v2 — Round 3

**Devil's-advocate verdict:** fatal=True · KILL — the episode-throughput constraint is mathematically fatal: with 3-5 minutes per rollout and group=16 for GRPO, the overnight window yields fewer than 20 training steps, which is provably insufficient for a visible base-vs-trained delta by 1 PM Sunday; every other attack (static single-module task set, gameable gate-count band, EDA toolchain bootstrap risk) compounds a project that the team's own phase13 spec already designates as slide-only insurance and should remain exactly that.

| # | Vector | Attack (severity) | Defender: holds? | Rebuttal / Fix (residual) |
|---|---|---|---|---|
| 1 | RL: Episode throughput k | The reward band target of 20-50% and a visible base-vs-trained delta by 1 PM Sunday requires enough GRPO steps to move the curve. One episode of the cocotb_dv grader runs 1 golden cocotb invocation plus 5 mutant cocotb i (5) | True | The attack lands directionally but mis-grounds the per-episode cost by conflating the DV-track grader with the repair-track grader. The 6x180s cocotb chain is in stream_arb_fifo_cocotb_dv/donotaccess/grade.py (lines 234-251) and only fires when the agent WRITES TESTBENCHES. SILICON-FORGE writes RTL- (res 3) |
| 2 | Researcher: It is not an | The phase13 spec promises 'spec templates parameterized (bit-widths, op sets) -> infinite tasks.' The actual task_catalog.py contains exactly three TaskSpec objects: stream_arb_fifo_repair, stream_arb_fifo_cocotb_dv, and (4) | True | Confirmed against task_catalog.py lines 23-66: exactly three TaskSpec objects, all module='stream_arb_fifo', differing only by track (repair/verification/formal). There is no parameterized sampler, no bit-width sweep, no op-set variation in the cloned repo. The 'infinite tasks' claim in phase13 is a (res 3) |
| 3 | Verifier: The synthesis  | The repair grader's synthesis_score() awards gate_in_band credit if cell_count is between 60 and 1400 cells (grade.py lines 213-214). The gate band is 23x wide. A model that learns to emit a structurally valid but semant (4) | False | This attack is materially wrong on the code. grade.py lines 283-285 impose a HARD CAP: `if float(functional['score']) == 0.0: reward = 0.0`. A constant-output module fails all 4 hidden-testbench scenarios -> functional_score = 0.0 -> reward forced to EXACTLY 0.0, which ERASES all synthesis and lint  (res 2) |
| 4 | YC: It is not a company, | The startup pitch is 'EDA incumbents and silicon startups want it.' But the research shows Cadence acquired ChipStack (a 20-person team doing RTL verification) for an undisclosed sub-$100M sum. That is an acqui-hire, not (4) | True | The market-shape critique is fair and the research corroborates it: Wing VC explicitly excludes chip design as a buyer target; Cadence's ChipStack purchase was a sub-$100M acqui-hire, not a recurring license; the agent-product layer (ChipAgents $74M, Cognichip $93M, Synopsys.ai) is crowded and may b (res 4) |
| 5 | Demo: Nobody in the room | Protean's demo is 'live 4.2ms -> 2.0ms speedup' — a number any engineer reads instantly. SILICON-FORGE's demo is 'timing: MET + gate-count chart flips green.' The demo requires the audience to understand what Yosys  (3) | True | Legibility is genuinely weaker than Protean's '4.2ms -> 2.0ms' and phase0 rates WOW 3/5 (lowest among finalists). But 'incomprehensible to 80% of the room' overstates it. The demo can be made legible without EDA knowledge: (1) a base-vs-trained pass-rate curve on hidden testbench scenarios (4/4 (res 3) |
| 6 | Time: The cocotb_dv trac | The cocotb_dv grade.py copies the hidden runner out of /donotaccess, stages it in a temp dir, chowns the tree to uid 1000, then invokes Verilator compilation followed by cocotb sim under setpriv. This chain requires: Ver (3) | True | The dependency chain is real (tool_env() probes Path.home()/utils/oss-cad-suite/bin; cocotb grader needs vendor/basejump_stl, line 179; formal needs SymbiYosys) and grader.py lines 52-70 fail-closed to reward 0.0 on ANY exception — so a missing tool silently produces all-zero rewards and GRPO collap (res 2) |
| 7 | Sponsor: HUD is load-bea | SILICON-FORGE's grader is entirely CPU-bound: Verilator compilation, cocotb Python simulation, and Yosys synthesis all run on CPU. The reward function requires no GPU. The research confirms: 'Yosys gate-count as the sole (2) | True | Accurate: the EDA grader (Verilator/Yosys/cocotb) is CPU-bound and needs no H100, so the $250 Modal and $25 GCP GPU credits add little to the GRADER. But the framing that Modal/GCP are 'decorative' misreads the architecture — the GPU compute is for the TRAINER/INFERENCE side (the GRPO policy model s (res 2) |
| 8 | Competition: Alpha-RTL ( | The research surfaces Alpha-RTL (arXiv 2606.05253, June 3 2026 — 17 days before this hackathon) as showing RL test-time training reduces PPA by 65% vs 26% for frozen-policy baselines on Verilog RTL. ChipSeek (ACL 2026) u (2) | False | Both are research ARTIFACTS (single training runs on fixed task sets), not reusable HUD environments with the uid-wall isolation, dynamic hidden-grader loading, and (once built) parameterized task sampler. The honest differentiation is precisely what the research already states: ChipSeek/VeriReason/ (res 2) |

**Defender overall: survives=True**
